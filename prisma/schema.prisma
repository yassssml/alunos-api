generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
}

model Aluno {
    id        Int      @id @default(autoincrement())
    nome     String?
    escola   String
    turma    String
    foto     String

    @@map("aluno")
}
